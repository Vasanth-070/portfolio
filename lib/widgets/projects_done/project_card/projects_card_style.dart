import 'package:flutter/material.dart';

class ProjectsCardStyle {
  final imageSize = const Size(480, 384);
  final openIconSize = 24.0;
  final wrapCrossGap = 8.0;
  final wrapinternalGap = 8.0;
  final verticalSpace = 24.0;
  final textBoxWidth = 480.0;
  final imagePadding = const EdgeInsets.all(48);
  final imageRadius = const BorderRadius.all(Radius.circular(12.0));
}
